repos:
  - repo: local
    hooks:
      - id: backend-tests
        name: Backend TDD Check (pytest)
        entry: bash -c "cd backend && python -m pytest"
        language: system
        types: [python]
        pass_filenames: false
        stage: [commit]